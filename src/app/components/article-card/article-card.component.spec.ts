import { ArticleCardComponent } from './article-card.component';

describe('ArticleCardComponent', () => {
  it('should create', () => {
    expect(new ArticleCardComponent()).toBeTruthy();
  });
});
